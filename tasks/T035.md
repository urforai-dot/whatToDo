# T035 — 달력 바에 담당자명 표시

- **단계(phase)**: misc
- **상태**: done
- **예상 소요**: 30분
- **선행 작업(dependencies)**:
- T033

## 설명

간트 바(T033)에 업무명만 있어서 누가 하는 업무인지 달력만 보고는 알 수 없었다. 바 텍스트에 담당자명을 이어붙인다. (2026-09-17 사용자 지시)

## 목표

달력 하나만 보고 "누가 뭘 하고 있는지"까지 한눈에 파악할 수 있게 한다.

## 완료 조건 (Acceptance Criteria)

- [x] 바 텍스트가 `업무명 · 담당자명`으로 표시됨 (담당자 여러 명이면 쉼표로 나열, 없으면 "미배정" — TaskListWidget/DueSoonWidget과 동일한 표기)
- [x] hover 툴팁(`title`)과 스크린리더용 `aria-label`에도 담당자명이 포함됨
- [x] 브라우저 + 실 데이터로 확인: 단일 담당자, 복수 담당자("이준호, 정하늘"), 미배정 세 경우 전부 정상 표시됨

## 메모

`Task.owners`는 이미 API가 내려주고 있어서(다른 위젯도 다 씀) 프론트만 수정. `ownerLabel()` 헬퍼 하나 추가(`src/components/dashboard/calendar-widget.tsx`).
