# T004 — DB 프로비저닝 (Postgres)

- **단계(phase)**: 프로젝트 초기 설정 (setup)
- **상태**: in_progress
- **예상 소요**: 30분
- **선행 작업(dependencies)**:
- T003

## 설명

Supabase MCP(또는 Supabase 대시보드/CLI)로 프로젝트를 만들고 Postgres 연결 정보와 API 키를 로컬/Vercel 환경변수에 설정한다. (2026-09-17: DB를 Neon/Vercel Marketplace 대신 Supabase로 확정 — `.claude/rules/skills.md` "DB: Supabase" 참고)

## 목표

업무 데이터를 영속적으로 저장할 DB를 준비한다.

## 관련 요구사항 (개발요구사항문서.md)

> STEP 4 - 업무/보고서 데이터 저장 필요

## 완료 조건 (Acceptance Criteria)

- [x] Supabase 프로젝트가 생성됨 (MCP 또는 대시보드/CLI) — 기존에 연결되어 있던 프로젝트(tdhuxsztpcgadonzsrxs) 사용
- [x] DATABASE_URL, SUPABASE_URL, SUPABASE_ANON_KEY 등 환경변수가 로컬(.env.local)에 등록됨
- [ ] 위 환경변수가 Vercel 프로젝트 설정에도 등록됨 — `vercel env add`가 auto-mode 시크릿 쓰기 정책에 막혀 사용자 승인 대기 중
- [x] psql 또는 클라이언트로 DB 접속이 확인됨 (`postgres` 클라이언트로 `SELECT 1` 성공)

## 메모

- 연결된 Supabase 프로젝트에 이미 다른 스키마(members/tasks/task_assignees, 한글 status)로 된 실제 데이터(섹션원1~10, 업무 4건, 2026-09-16 생성)가 있었음. 사용자 확인 후(2026-09-17) 기존 데이터를 지우고 이 저장소의 Drizzle 스키마(users/tasks/task_owners/comments)로 덮어씀 — `drizzle/0000_rainy_tusk.sql` ~ `0003_task_check_constraints.sql`을 Supabase MCP로 순서대로 적용.
- Vercel 환경변수 등록은 `vercel env add`가 시크릿 스토어 쓰기라 auto-mode 정책에 걸려 자동 실행이 막힘 — 사용자가 직접 실행하거나 권한을 허용해줘야 함. 값은 아래 메모 대신 `.env.local`에 있음(레포에는 커밋 안 됨).
