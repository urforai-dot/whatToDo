# T015 — 간단 사용자 식별(로그인) 구현

- **단계(phase)**: 사용자 식별 (auth)
- **상태**: todo
- **예상 소요**: 30분
- **선행 작업(dependencies)**:
- T007

## 설명

섹션원 목록에서 본인을 선택하는 방식의 간단한 로그인/세션 처리를 구현한다 (복잡한 인증 없이 신뢰 기반).

## 목표

핵심 시나리오 1(서비스 접속)을 충족하는 최소한의 사용자 식별 수단을 제공한다.

## 관련 요구사항 (개발요구사항문서.md)

> STEP 4 - 핵심 시나리오 1: 서비스 접속

## 완료 조건 (Acceptance Criteria)

- [x] 섹션원 목록(/login)에서 본인을 선택하면 httpOnly 쿠키(session_user_id)에 식별 정보가 저장됨
- [x] 이후 요청에서 현재 사용자를 식별할 수 있음 (`src/lib/session.ts`의 `getSessionUserId()`) — 코멘트 작성 API가 이걸로 작성자를 정하므로, 클라이언트가 authorId를 직접 보내 다른 사람 행세를 할 수 없음 (adversarial-reviewer 지적: 처음엔 session.ts를 만들어두고 아무데서도 안 썼음)

## 메모

(작업 중 특이사항, 결정 사항, 사람 판단이 필요한 이유 등을 이곳에 기록)
