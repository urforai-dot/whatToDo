# T028 — 빈 상태/에러 상태 처리

- **단계(phase)**: 폴리싱 & QA (polish-qa)
- **상태**: done
- **예상 소요**: 30분
- **선행 작업(dependencies)**:
- T017
- T022

## 설명

데이터 없음, API 에러 등 예외 상황에 대한 UI 처리를 추가한다.

## 목표

예외 상황에서도 사용자가 당황하지 않도록 한다.

## 관련 요구사항 (개발요구사항문서.md)

> STEP 4 - 제약: 1인 바이브 코딩 (최소 품질 기준 확보)

## 완료 조건 (Acceptance Criteria)

- [x] 업무/코멘트/담당자 각 위젯에 빈 상태 안내(`EmptyRow`)가 있음
- [x] API 실패 시 에러 안내(`role="alert" aria-live="polite"`) 및 재시도 옵션이 표시됨 — **실제 브라우저로 확인함**: DB가 아직 연결 안 된 상태라 모든 API가 실패하는데, 대시보드/이력 페이지 모두 흰 화면이나 크래시 없이 "요청에 실패했습니다 (500)" + 다시 시도 링크가 정상적으로 뜸

## 메모

DB 연결 전인 지금 상태 자체가 "API 실패" 시나리오를 그대로 재현해줘서, 별도로 에러를 흉내낼 필요 없이 실제로 검증됐다.
